<%- include('../layout/user/header') %>

    <div class="container">
        <div class="row vh-100 justify-content-center align-items-center ">
            <div class="col-4 text-center ">
                <div class="d-flex justify-content-between mb-3">
                    <div id="timer" class="text-white ">
                        00:10
                    </div>
                    <button id="resendOTP" class="d-none btn btn-light loginButtonColor text-white ">Resend The
                        OTP</button>
                </div>
                <form action="/userOtpVerificationCode" method="post">
                    <% locals.errorMessages?locals.errorMessages:'' %>
                        <% if(locals.errorMessages) {%>
                            <h6 class="text-danger ">
                                <%= locals.errorMessages %>
                            </h6>
                            <% } %>
                                <input class="form-control mb-3 " type="text" name="otp" placeholder="enter the otp"
                                    required>
                                <button type="submit" class="btn btn-primary loginButtonColor w-50">Submit</button>

                </form>
                </form>
            </div>
        </div>
    </div>
    <script src="/javaScript/user/userOtpVerificationCode.js"></script>
    <%- include('../layout/user/footer') %>